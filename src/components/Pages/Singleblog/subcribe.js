import React from 'react';
import { Modal, Button } from 'react-bootstrap';

const SubcribeModal = ({ show, handleClose }) => {
    return (
        <Modal show={show} onHide={handleClose}>  
        </Modal>
    );
};

export default SubcribeModal;
